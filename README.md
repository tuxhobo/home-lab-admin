# home-lab-admin
ansible project to automate deployments in my home-lab

The test_vm_create branch is a trial of vm template deployment that is not working.
It will likely be abandoned but keep this work history around for now.

